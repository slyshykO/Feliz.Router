{
    "private": true,
    "scripts": {
        "build:demo": "dotnet tool restore && dotnet fable demo --run webpack --mode production",
        "start:demo": "dotnet tool restore && dotnet fable watch demo --runFast webpack-dev-server",
        "pretest": "dotnet tool restore && dotnet fable tests",
        "test": "mocha tests"
    },
    "repository": {
        "type": "git",
        "url": "https://github.com/Zaid-Ajaj/Elmish.Toastr.git"
    },
    "devDependencies": {
        "mocha": "^11.7.0",
        "remotedev": "^0",
        "webpack": "^5.75.0",
        "webpack-cli": "^6.0.1",
        "webpack-dev-server": "^5.0.0"
    },
    "dependencies": {
        "react": "^19.2.0",
        "react-dom": "^19.2.0"
    }
}
